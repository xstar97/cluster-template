aliases:
  updateFlux: "flux reconcile source git cluster cluster --verbose"
  k: "kubectl"
  f: "flux"
  gto: "gitops"
  ct: clustertool
  test-nvidia: "/workspaces/cluster/scripts/talos/nvidia-smi.sh"
